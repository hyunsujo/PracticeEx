#include <stdio.h>

int main()
{
	char a;
	a= 'q';
	
	//printf("%d, %c \n" ,a ,a);
	
	printf("%c의 ASCII Code 값이 %d 입니다.\n",a,a);
	
	//printf("'a'의 값과 저장되어 있는 문자는 ? 값 : %d, 문자 : %c \n",a,a);
	
	return 0;
}